// Objective: Define the interface for the projects data

export interface ProjectProps {
    id: number;
    name: string;
    description: string;
    image: string;
    url: string;
    alt: string;
  }